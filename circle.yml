machine:
  python:
    version: 2.7.3
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
    - /home/ubuntu/aws

  pre:
    - sudo apt-get update; sudo apt-get install rpm
    - ./build_container.sh

test:
  override:
     - echo "test"

deployment:
  develop:
    branch: master
    owner: XaroRSA
    commands:
      - ./deploy.sh
      - rsync -avz ./dist circleci@188.166.167.140:/tmp
      - ./trigger_grafana_docker_build.sh ${TRIGGER_GRAFANA_DOCKER_CIRCLECI_TOKEN}
